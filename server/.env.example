DATABASE_URL=libsql://<your-database-url>
DATABASE_AUTH_TOKEN=

WEBSITE_HOST=https://yourcontextwebsite

DASHBOARD_SECRET=
# (optional)
# DASHBOARD_IP_WHITELIST=
# (optional)
# API_WRITE_IP_WHITELIST=111.111.111.111
API_WRITE_SECRET=
# (optional)
# API_READ_IP_WHITELIST=
CRON_SECRET=

# Set to "true" if behind a reverse proxy (nginx, Caddy, etc) that sets
# X-Real-IP / X-Forwarded-For. Without this, IP whitelists won't work outside
# of Vercel. Don't enable unless you trust your proxy — these headers are
# trivially spoofable by clients when there's no proxy.
# TRUSTED_PROXY=true

# Comma-separated list of phone numbers/emails allowed to receive iMessages.
# If unset or empty, iMessage sending is completely disabled (safe default).
# Use * to allow all recipients.
# IMESSAGE_ALLOWED_RECIPIENTS=+15551234567,+15559876543,friend@icloud.com

# If you're looking for the E2E decryption key, it doesn't live inside of this
# app at all. (That's what makes it E2E :)

# Data retention (in hours). 0 or unset = no automatic expiration.
SCREENSHOT_RETENTION_HOURS=1
# IMESSAGE_RETENTION_HOURS=0
# WHATSAPP_RETENTION_HOURS=0
# CONTACT_RETENTION_HOURS=0
# LOCATION_RETENTION_HOURS=0
# STICKIES_RETENTION_HOURS=0
# LOG_RETENTION_HOURS=0